from setuptools import setup, find_packages

setup(name='internal-models',
      version='0.0.2',
      description='Models package',
      packages=["models"],
      zip_safe=False,
      install_requires=[])
